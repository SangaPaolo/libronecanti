import React from "react";
import { Text, View } from "react-native";

const SearchBarList = () => {
	return (
		<View className="absolute size-full bg-secondary rounded-[6px] flex items-center justify-center z-30">
			<Text>SearchBarList</Text>
		</View>
	);
};

export default SearchBarList;
